---
topic: Elevator Simulator frontend visual redesign
updated: 2026-09-24T19:10
---

- (decision) Source scan: user selected epics.md and prd.md as inputs; skipped ARCHITECTURE-SPINE.md.
- (decision) Style direction: Modern dashboard/SaaS (Vercel/Linear/Stripe-like), clarity-first. User explicitly chose deeper visual investment (smooth tween animation for elevator movement per NFR-4, dark mode, higher visual detail) despite PRD's guidance that UI polish ranks below correctness/tests/Docker and deadline is 2026-09-25 (tomorrow) -- flagged and accepted knowingly.
- (decision) Color theme: Light mode, blue accent (Stripe-dashboard-like).
- (decision) Building view: vertical shaft/well layout - 3 columns (one per elevator shaft), cabin cells slide up/down across 10 floors. This is the visual centerpiece.
- (decision) Connection status affordance: small corner badge, 3 states (connecting/connected/disconnected) mapped 1:1 to existing frontend ConnectionStatus type in uiSlice.ts.
- (decision) ui-ux-pro-max lookups: color palette = 'Analytics Dashboard' profile (primary #1E40AF, secondary #3B82F6, accent #D97706 amber, bg #F8FAFC, destructive #DC2626). Typography = 'Friendly SaaS' (Plus Jakarta Sans, single family, modern Inter alternative). Icons = Phosphor library (CaretUp/CaretDown or ArrowUp/ArrowDown for direction, standard Phosphor names for door/connection not in curated DB -- using library convention directly). Animation = CSS transform transition (simpler/safer than GSAP for React-driven 500ms tick position updates) as primary; GSAP Flip noted as fancier optional alternative.
- (decision) Confirmed: Analytics Dashboard palette (amber accent kept) and Plus Jakarta Sans typography, both as-is from ui-ux-pro-max lookup.
- (event) spines drafted: DESIGN.md (Ascent - light mode SaaS dashboard, blue/amber/green/red semantic palette, Plus Jakarta Sans) and EXPERIENCE.md (single-surface dashboard, shaft-view IA, Alex demo journey) written directly from logged discovery decisions.
- (event) Reviewer Gate skipped per user (deadline pressure, decisions already validated during discovery). Spines finalized, status: final. Proceeding directly to implementation in the frontend workspace.
- (decision) Update trigger: user reports too much empty space, wants a more beautiful/modern/friendly feel. Direction chosen: fill space with visual richness/content (not just tighten spacing); keep instrument-panel spirit but warmer/softer (softer elevation, slightly richer color use); priority fix is the page-level shell -- dashboard currently sits pinned top-left with the rest of the viewport left blank, rather than filling/framing the available width.
- (decision) Reviewed key-dashboard.html mock v1: user flagged that the docked Destination Panel + Door Controls, previously stacked below the open cabin's shaft column, pushed that column taller and forced page-level scroll -- unacceptable, wants everything on one screen. Fixed: docked panel is now an absolutely-positioned overlay anchored inside the shaft's own bounds, docking to whichever edge (top/bottom) is farther from the cabin, so opening a door never changes page height. Given its own higher elevation tier (--shadow-overlay) as a documented exception to the single-shadow-weight rule, since it is a transient floating overlay, not a static card. Row height trimmed 56->52px to reclaim vertical budget.
- (decision) User asked to confirm: pressing a hall-call up/down button should send an elevator to that floor and show the destination panel there, instead of the floor just sitting static. Clarified this is already the existing spec'd + implemented behavior (EXPERIENCE.md Flow 1 steps 3-6; backend Dispatcher/NearestCarStrategy already assigns nearest idle car). Made key-dashboard.html mock click-through-interactive (vanilla JS, demo-only, not shipped) so the user can literally press a hall call and watch the assigned car travel there and the new overlay-style Destination Panel appear -- validates the redesigned overlay panel under real interaction, not just static frames. Noted explicitly in-file that the real React client stays fully server/Snapshot-driven (AD-1/FR-13); this JS never ships.
- (decision) Superseded the docked-overlay Destination Panel entirely per user feedback: they want floor selection to feel more authentic, like a real elevator's one-button-per-floor car panel. New design: while a car's doors are open, every OTHER floor-cell inside that car's own shaft column becomes a real clickable button (aria-label 'Car call floor N', matching the real DestinationPanel component's existing label convention) -- no separate floating panel at all. Door Controls (Hold/Close) moved out of the shaft into that car's header row next to its status chip, always laid out there (toggled via visibility, not display) so the header height never changes either. This also fully removes the earlier top/bottom docking-edge logic since there is no more panel to position.
- (decision) Corrected misreading of the previous 'chan that hon' request: user meant Destination Panel + Hold/Close should stay grouped as ONE cluster (not scattered one button per floor-row), anchored to grow flush from the cabin's OWN row (e.g. arriving at floor 10 -> panel grows straight down starting right below row 10), not pinned to the shaft's outer top/bottom edge. Asked 3 clarifying questions (with ASCII previews) before rebuilding: confirmed placement stays per-car inside the shaft (not merged into the Hall Call rail), reasoning explicitly given -- two cars can stop at the same floor simultaneously, so identity must stay tied to the shaft column. Also confirmed: if that rare same-floor-two-cars case happens, show each car's cluster separately (trivially satisfied since each lives in its own shaft column already). Rebuilt car-panel positioning in key-dashboard.html to anchor top/bottom flush against the cabin's computed row instead of the shaft's fixed edges.
- (decision) Further simplified per user: remove the 'Send to floor' label and the separate small colored cabin digit box entirely. Now the instant doors open, the cabin box at that row is hidden and the panel's own colored header (floor number + Hold/Close) takes over that EXACT row with zero gap -- the panel effectively replaces the cabin digit in place. The 1-10 floor grid grows from there. All 10 floor buttons render (not 9, excluding current) with the current floor's own button shown lit/disabled, matching a real elevator car panel's convention of always showing every floor button.
- (decision) Removed the absolute-positioned car-panel overlay entirely per user request -- controls must render inline, in normal document flow, inside the actual floor-cell, not position:absolute. Row height stays fixed at --row-height (never grows taller); instead the car's whole shaft column widens via flex-grow (shafts container changed from CSS grid equal-thirds to flex with a .car--expanded modifier ~2.6:1:1 ratio, animated, other two columns shrink to compensate) so Hold/Close + all 10 floor buttons fit on one horizontal line within that single row. Local overflow-x:auto on that row only, as a safety net, not the expected path.
- (decision) Added symbolic climbing trail: while a car travels, each floor-cell it steps through briefly lights up blue (matching the cabin's in-transit color) then fades, synced to the same per-floor step as the cabin's own translateY tween -- reinforces motion/direction beyond the single sliding cabin box. Also widened the page shell (max-width 1120->1280px) and trimmed side padding (space-5->space-4) per user request to reclaim room for content, especially the expanded shaft column introduced by the inline floor-cell panel.
- (decision) Clarified via AskUserQuestion: user's complaint was that the small floating cabin box's transit-only up/down arrow icon (2 possible icons) wasn't enough to clearly place 3 separate cars at rest. Chose to KEEP the sliding cabin box for transit animation, and ADD a persistent tint + left accent bar on the floor-cell where a car is idle+doors-closed (paintPosition/clearPosition, hooked into travelTo start, closeDoors, showPanel, and initial paint) so resting position reads clearly from the floor-cell itself, not just the small box.
- (decision) Ran a color/polish review per user request via ui-ux-pro-max (python unavailable on this machine, so consulted the skill's own data/colors.csv and data/typography.csv catalogs directly). Finding: every Ascent token already matches the catalog's 'Analytics Dashboard' profile almost exactly (confirms the original bmad-ux session's lookup was sound), EXCEPT Open Green -- computed WCAG contrast of white text on #16A34A is only ~3.3:1 (fails the 4.5:1 floor), affecting the connection badge, cabin fill, and the new floor-cell panel header. Fix applied in key-dashboard.html: darkened --color-open to #15803D (~5:1 with white, same hue family). This is a real DESIGN.md token fix, not mock-only -- the shipped React app's ConnectionBadge.css and ElevatorCar.css have the identical white-on-#16A34A pattern already. Also nudged ambient floor-cell number opacity 0.55->0.7 for legibility. Typography (Plus Jakarta Sans, 'Friendly SaaS' pairing) reconfirmed as an appropriate catalog match, unchanged. Still needs: user sign-off to carry #15803D into DESIGN.md's frontmatter + body text and into the real component CSS files.
- (decision) Fixed row misalignment per user request: each shaft column had an extra id+chip header (.car__head) that the Hall Call rail lacked, offsetting every floor row below it. Replaced both top-level section headings (Hall Calls / Shaft View, each with a descriptive subtitle) and .car__head with one shared .col-head class at a fixed 24px height (no subtitle), used identically above the rail and above every shaft column -- guarantees floor N's rail row and floor N's shaft cell sit at the same Y by construction rather than by matching two different elements' font metrics.
- (decision) Root-caused why the resting-position cue (change #8) read as too faint: the cabin box is position:absolute and nearly fills its own floor-cell's width/height, so it sits ON TOP of and hides almost all of the row-level tint meant to signal 'car is here'. Fixed at the actual visible layer: added .cabin--here, a blue ring/glow directly on the cabin box itself (toggled by paintPosition/clearPosition alongside the row class), since the cabin box is the one part of that row that's never obscured.
- (decision) Fixed the cabin--here ring bleeding into neighboring floor-cell rows: it was an outer box-shadow (spread+blur), and since nothing clips between individual rows (only around the whole shaft), it visually spilled a few px above/below. Asked user which containment style they wanted (inset glow vs solid border), confirmed inset glow. Switched to box-shadow: inset ring (white, 2px solid + soft inset blur) -- geometrically cannot exceed the cabin's own box, so it never encroaches on another floor even if a second car is resting one row away.
- (decision) Fixed shaft column width growing when floor-cell--here appeared: its floor number had become a padded pill (min-width:22px + 8px horizontal padding), extra content-width demand that pushed the .car flex item wider. Reverted the number back to plain bold colored text (matching floor-cell--pass's treatment) with no padding/min-width -- the cabin's own inset ring already carries the 'here' signal (per the prior two fixes), so the row-level number never needed to be a badge. Column width now stays identical whether or not --here is applied.
- (decision) REVERSED the shaft-column-widening-on-doors-open mechanism (introduced a few turns ago as the agreed alternative to growing row height). User saw it live and rejected it: asked to confirm via AskUserQuestion whether they wanted the expansion kept (fixing a stray padding bug) or removed entirely -- confirmed removed entirely, all 3 columns must stay exactly equal width always. Since row height also cannot grow (earlier hard constraint, still in force), and Hold/Close + 10 floor buttons need ~430px while a column is only ~250-320px, the doors-open row now scrolls horizontally within itself (overflow-x:auto with a visible thin scrollbar as the affordance, plus scroll-snap-type so buttons land cleanly) instead of resizing anything around it. Removed .car--expanded (CSS class + both JS toggle sites in showPanel/hidePanel) entirely -- dead code, not kept as an unused option.
- (decision) Per user request: removed the 'Fl N' label and Hold/Close buttons from the floor-cell panel row entirely -- it now shows only the 10 floor buttons (less content to horizontally scroll through, per the prior fixed-width-column constraint). Hold/Close moved up into each car's col-head (right of the status chip), rendered once at buildShafts time and always laid out there (visibility toggled via new .col-head__doorctrls.is-active class + paintDoorCtrls(), not display, so col-head's fixed 24px height never changes) -- only interactive while that car's doors are actually open. Removed now-dead .mini-btn/.floor-cell__floor-label/.floor-cell__ctrls CSS and markup.
- (decision) Made floor-cell--here easier to see per user request. Asked whether to grow just that row's height (risking re-breaking row alignment fixed a few turns ago) or bump --row-height globally; confirmed global. Changed --row-height 52->58px (CSS var + JS ROW const, both updated in lockstep) -- applies uniformly to the Hall Call rail and every shaft row so alignment across columns stays intact. Also nudged --here's own tint opacity (0.16->0.2), left-border (4->5px) and number font-size (12->13px) slightly stronger now that there's a bit more vertical room.
- (decision) Per user request, widened shaft columns to fit the 10-button floor-cell panel row without scrolling on a normal window: set .car min-width to 344px (computed from 10*28px buttons + 9*4px gaps, gap trimmed from 6->4px, + row padding + buffer), widened .page max-width 1280->1420px to give the extra 3 columns room, kept min-width:0 override at the <=560px mobile breakpoint so stacked layout still works. The horizontal-scroll fallback from the prior change stays in place (.shafts itself now also scrolls, contained -- not the whole page) for windows narrower than the fit-width, so nothing regresses there; it's just no longer the expected path on a normal-size window.
- (event) Implemented the approved mock into the real React app: frontend/src/App.tsx + App.css (page shell, stat-strip header, board grid, 560px stacked-column breakpoint), index.css (color-open -> #15803D, two-layer shadow-card, radius-lg on containers, shared .col-head/.col-title), designTokens.ts (ROW_HEIGHT_PX 48->58), BuildingView/FloorHallPanel (rail restyle to icon-btn/rail-row), ElevatorCar (col-head with status chip + always-mounted DoorControls, the open cabin's own floor-cell becomes the DestinationPanel row in place of the cabin digit, resting-position ring/tint, climbing-trail flash driven off currentFloor changes -- real snapshots move exactly one floor per tick per Elevator.moveOneFloor, so this is Snapshot-derived, not simulated), DestinationPanel (all 10 floor buttons including current floor, shown lit/disabled), DoorControls (new isOpen prop, always mounted with disabled + visibility toggle instead of conditional mount, so col-head's height never changes). Updated DestinationPanel.test.tsx/DoorControls.test.tsx/ElevatorCar.test.tsx to assert toBeDisabled()/not.toBeDisabled() where an element is now always mounted rather than conditionally rendered. All 60 frontend tests pass; tsc -b + vite build clean. DESIGN.md and EXPERIENCE.md updated to match (color token, row height, layout/component descriptions, IA table, Component Patterns table, State Patterns table) per user's explicit request to sync docs alongside code.
