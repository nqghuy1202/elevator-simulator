---
scope: Elevator Simulator system (Node.js backend + React frontend), 3 elevators / 10 floors
purpose: build-substrate
altitude: feature
updated: 2026-09-23T21:36
---

- (version) Node.js 24 LTS 'Krypton' (24.21.0) is current LTS as of 2026-09, verified via web (nodejs.org release schedule)
- (version) React 19.3.0 is current stable as of 2026-09, verified via web (react.dev/versions)
- (version) Socket.IO 4.8.3 (server+client) is current as of 2026-09, verified via npm
- (version) Redux Toolkit 2.12.0 is current as of 2026-09, verified via npm
- (question) TypeScript 7.0.2 (new Go-based compiler, released 2026-07) is technically 'latest' but is a major toolchain rewrite; TS 5.x line may be safer for a 2-day deadline given ecosystem/tooling maturity — needs user decision before pinning Stack table
- (decision) TypeScript: use latest 5.x line, not 7.0.x (Go-rewrite) — stability over novelty given 2-day deadline
- (decision) Repo structure: npm workspace (root package.json, backend+frontend+shared as workspaces) instead of 2 independent folders — chosen specifically to make the WS/Snapshot type contract single-sourced
- (decision) Paradigm: Hexagonal core (ports-and-adapters) for backend domain layer + server-authoritative simulation for client relationship. Alternatives considered: (A) server-authoritative alone (didn't name backend internal boundary), (C) plain layered (weaker enforcement of domain purity/testability NFR). User did not object to PM's lean toward B; proceeding.
- (decision) All 5 proposed ADs confirmed by user as-is: AD-1 domain/ws dependency direction + server-authoritative client, AD-2 Elevator state mutation ownership, AD-3 Redux snapshot replace-not-merge, AD-4 shared/ package single-sources WS/Snapshot types, AD-5 Dispatcher depends only on SchedulingStrategy interface
- (decision) Frontend build tool: Vite (create-react-app is officially deprecated since Feb 2025, Vite is React team's current recommendation for client-only builds), verified current v8.3.0
- (decision) Test framework: Vitest (5.0.1) for both backend and frontend unit tests — pairs natively with Vite, avoids mixing Jest+Vitest configs across the npm workspace, satisfies PRD's testability NFR (NearestCarStrategy + state machine)
- (version) react-redux 9.3.0 verified compatible with React 19 (peer-dep fixed, no --legacy-peer-deps needed)
- (event) Spine draft distilled (5 AD, paradigm, conventions, stack, structural seed, capability map, deferred list) — entering input reconciliation
- (event) Reconciliation: architecture-doc reconciliation found zero gaps (spine is a consistent, non-contradictory distillation). PRD reconciliation found 3 gaps: FR-7 re-evaluation trigger/ordering unaddressed, FR-9 Stop Queue no-overshoot ordering invariant unaddressed, FR-15 concurrency isolation miscited to AD-2 which doesn't substantiate it
- (decision) Autofix: add AD-6 (Stop Queue insertion preserves direction-order, closes FR-9 gap), AD-7 (Dispatcher.reevaluatePending runs once per tick after all Elevators tick, closes FR-7 gap), AD-8 (Elevators share no mutable state, Building iterates sequentially, closes FR-15 gap); fix Capability Map F7 row to cite AD-8 instead of AD-2
- (version) Nginx stable Docker image verified via Docker Hub: nginx:1.30-alpine (1.30.5) is current stable tag as of 2026-09
- (version) Docker Compose: search results for exact current version number were inconsistent/unreliable; current best practice confirmed is to use the 'docker compose' CLI plugin (v2 syntax) and omit the compose-file 'version:' key entirely (deprecated) rather than pin an unverifiable version number
- (event) Reviewer Gate: rubric walker PASS (0 critical, 2 medium, 2 low, 1 note), version-verification CONDITIONAL PASS (Docker/Nginx unverified), adversarial found 14 findings (6 High). All findings closed via wording-only tightening: AD-1 (interpolation carve-out), AD-2 (return contract + single-writer-per-transition), AD-3 (slice scoping + sequence-number ordering guard), AD-4 (shared/ ownership + sequence number), AD-5 (Car Call intentionally outside Strategy scope), AD-6 (single insertion+removal method, IDLE-seeding, dedup/current-floor guard), AD-7 (pull-based reevaluation, forbids mid-loop push, FIFO tie-break), AD-8 (reconciled with AD-7 on iteration-order framing, clarified Strategy reads via read-only accessors). Plus: verified Nginx/Docker Compose versions, added WS-connect-immediate-snapshot and container-networking conventions to close rubric's 2 medium findings. No open product/scope decisions remained — all fixes were spine-wording precision, applied without further user input.
- (event) spine finalized
