/**
 * Re-exported from `shared` (AD-4) — `shared/src/types.ts` is the canonical
 * source. This file exists so `domain/` import paths (`./DoorState.js`)
 * stay stable for existing callers.
 */
export type { DoorState } from 'shared/src/index.js';
