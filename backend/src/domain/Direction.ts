/**
 * Re-exported from `shared` (AD-4) — `shared/src/types.ts` is the canonical
 * source. This file exists so `domain/` import paths (`./Direction.js`)
 * stay stable for existing callers.
 */
export type { Direction } from 'shared/src/index.js';
